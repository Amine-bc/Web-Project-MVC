<-- ep 2 --> setup the core + views controllers router request response ..etc
* missed the first tasks bu in upcoming ones i will document them all inchalah *
TODO: add the get body of the get and post methods so we can get the params sent in the request
DONE
TODO: add the setLayout method ( in controller )
DONE
TODO: add the login and register
DONE
<--      -->

<-- ep 3 --> Models Forms Validation: create models + validation parts if errors display + add record in database
TODO: create auth controller
TODO: create register model ( public string first name, last name, email, password, password confirm)
validation don't do bad way by doing populating the errors list by checking if something is empty or doesn't conform to type and then rerender
the page with the errors
TODO: create abstract model class and then import it iit needs load_data() ( loop over the data check if key exists as property and then assign )
 & validate()
TODO: create addError() method used in validate and another errorMessages() that returns the error message mapped to the rule.
TODO: create rules abstract method and public const rules
TODO:
<--      -->

